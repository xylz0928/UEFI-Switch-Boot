# Windows / Linux 双系统 UEFI 启动项切换工具

本工具提供一组脚本，用于在 **Windows** 与 **Ubuntu（或其他 Linux）** 双系统环境下，**单次修改 UEFI 下次启动项**，实现一键重启进入指定系统，无需每次手动按热键或修改 BIOS 顺序。

---

## ✨ 特性

- **一键切换**：选择目标系统，自动设置下次启动顺序并重启。
- **自动识别**：通过 `bcdedit` 扫描 UEFI 固件中的系统标识符（ID），自动匹配 Windows 和 Ubuntu。
- **缓存机制**：成功扫描后保存标识符至本地缓存，后续启动无需重复扫描，提高效率。
- **手动预设**：若自动扫描失败，支持在脚本中预设 Ubuntu 的标识符作为备选。
- **交互友好**：彩色输出，超时默认选项（Ubuntu），支持重新扫描、进入 UEFI 固件设置等。
- **跨平台支持**：本项目包含 **Windows 批处理** 和 **Linux Shell** 两套实现，满足不同系统的操作需求。

---

## 📦 文件说明

| 文件名 | 适用系统 | 说明 |
|--------|----------|------|
| `switch-boot.bat` | Windows（UEFI） | Windows 端切换脚本（本文档主要介绍） |
| `switch-boot.sh` | Linux（Ubuntu） | Linux 端切换脚本（**待补充**） |

> 两份脚本可独立使用，也可分别部署在各自系统中，方便双系统用户从任意环境切换启动项。

---

## 🖥️ Windows 脚本使用说明

### 环境要求

- Windows 7 / 8 / 10 / 11（64 位，UEFI 启动模式）
- 管理员权限（脚本会自动检测并以管理员身份运行）
- 已安装 `bcdedit`（系统自带）

### 下载与准备

1. 将 `switch-boot.bat` 下载到本地任意目录（推荐放在非系统盘，避免误删）。
2. 右键点击该文件，选择 **“以管理员身份运行”**。

### 首次运行

- 脚本会自动扫描 UEFI 固件中的启动项，识别 Windows Boot Manager 和 Ubuntu 的标识符。
- 如果未找到 Ubuntu，会提示您手动输入标识符（格式如 `{xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx}`）。
- 扫描成功后，脚本会将标识符保存到同目录下的 `switch-boot.ini` 缓存文件中，下次运行可直接读取。

### 菜单选项

运行后显示如下菜单：
```
=============================================
           启动切换工具
=============================================

  当前配置
    Windows ID: {bootmgr}
    Ubuntu  ID: {xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx}
    缓存文件: C:\path\to\switch-boot.ini

=============================================
   选择下次启动系统
=============================================

  1 - Windows
  2 - Ubuntu（默认，10秒后自动）
  3 - UEFI 固件设置
  r - 重新扫描并更新缓存
  q - 取消
```
- 输入对应数字或字母后，脚本将设置 UEFI 的 `bootsequence` 并立即重启（除 `q` 取消外）。
- 若 10 秒无操作，默认选择 **Ubuntu**。

### 手动预设（备选方案）

若自动扫描始终无法获取 Ubuntu 标识符，可直接编辑脚本，修改以下变量：

    set "MANUAL_UBUNTU_ID={你的Ubuntu标识符}"

脚本会优先使用该预设值，并生成缓存文件。

### 缓存文件说明

- 文件名：`switch-boot.ini`（与 `.bat` 同目录）
- 内容示例：

    WIN_ID={bootmgr}
    UBUNTU_ID={xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx}

- 若缓存损坏或需重新扫描，可在菜单中选择 `r` 删除缓存并重新扫描，或直接删除 `.ini` 文件。

---

## 🐧 Linux 脚本（待补充）

本项目计划提供 Linux 端的切换脚本（使用 `efibootmgr` 实现），方便在 Ubuntu 下设置下次启动项。  
脚本将支持：

- 自动扫描 UEFI 启动条目，识别 Windows 和 Ubuntu。
- 交互式菜单选择切换。
- 缓存标识符，避免重复扫描。
- 重启或仅设置不重启等选项。

> 详细使用说明将在 Linux 脚本提交后补充至此文档。

---

## ⚠️ 注意事项

- **仅适用于 UEFI 模式**，传统 BIOS（Legacy）不支持。
- 必须以 **管理员/root** 权限运行，否则无法修改 UEFI 变量。
- 修改 `bootsequence` 仅影响 **下一次** 启动，重启后恢复为默认顺序。
- 若双系统使用 **GRUB** 作为主引导，则可能无需本工具（GRUB 本身可引导其他系统）。本工具适用于直接通过 UEFI 原生引导的场景。
- 如遇杀毒软件拦截，请放行或添加信任（脚本仅调用系统自带命令，无恶意行为）。

---

## 🛠️ 常见问题

**Q: 运行后提示“请以管理员身份运行”怎么办？**  
A: 右键脚本文件 → “以管理员身份运行”，或在命令提示符（管理员）中执行。

**Q: 扫描不到 Ubuntu 标识符怎么办？**  
A: 确保 Ubuntu 已正确安装并存在于 UEFI 启动列表中。可在 UEFI 固件设置中查看，或手动输入标识符（可通过 `bcdedit /enum firmware` 查找）。

**Q: 选择后没有重启，而是报错？**  
A: 检查是否以管理员权限运行，或确认 `bcdedit` 命令可用。可尝试手动执行 `bcdedit /set {fwbootmgr} bootsequence <ID>` 测试。

**Q: 缓存文件可以删除吗？**  
A: 可以，删除后下次运行会重新扫描。

---

## 📝 贡献与反馈

欢迎提交 Issue 或 Pull Request 改进脚本。  
项目地址：[GitHub 仓库链接]（待填写）

---

## 📄 许可证

[MIT License](LICENSE)

---

> **提示**：Linux 端脚本正在开发中，敬请期待……
